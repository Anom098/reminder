<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by the Flutter tool to talk to the VM service in debug builds. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
