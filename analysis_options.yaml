include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Documentation is mandatory for public API in this project.
    public_member_api_docs: warning
    # Treat common footguns as build failures.
    invalid_annotation_target: ignore
    missing_required_param: error
    missing_return: error
    todo: info
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"
    - ".dart_tool/**"

linter:
  rules:
    # --- Correctness ---
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    comment_references: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true

    # --- Style / maintainability ---
    always_use_package_imports: true
    avoid_bool_literals_in_conditional_expressions: true
    avoid_positional_boolean_parameters: true
    # avoid_redundant_argument_values is deliberately OFF. Passing a default
    # explicitly is often the clearest way to document a decision — e.g.
    # `requestAlertPermission: false`, which records that the app prompts for
    # notification permission later, on purpose.
    avoid_returning_this: true
    avoid_unused_constructor_parameters: true
    avoid_void_async: true
    cascade_invocations: true
    directives_ordering: true
    join_return_with_assignment: true
    omit_local_variable_types: false
    only_throw_errors: true
    prefer_asserts_in_initializer_lists: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_expression_function_bodies: false
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_single_quotes: true
    # require_trailing_commas is deliberately OFF: the Dart 3.8+ formatter owns
    # comma placement, and the two disagree.
    sort_constructors_first: true
    sort_unnamed_constructors_first: true
    unawaited_futures: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    use_named_constants: true
    use_setters_to_change_properties: true
    use_string_buffers: true

    # --- Documentation ---
    package_api_docs: true
    public_member_api_docs: true
    slash_for_doc_comments: true

    # --- Flutter specific ---
    use_build_context_synchronously: true
    use_colored_box: true
    use_decorated_box: true
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    sized_box_shrink_expand: true
